import styled from 'styled-components'

const Wrapper = styled.div`
  margin: 100px 0 0 0;

  & .community-container {
    display: flex;
  }

  /* LinkTab */
  & .linktab-container {
    display: flex;
  }
`

export default Wrapper