import styled from 'styled-components'

const Wrapper = styled.div`
  margin-top: 70px;

  & .upload-container {
    display: flex;
    padding: 2rem;
  }

  & .editor-container {
    // display: flex;
    padding: 0 2rem 0 0;
    // min-width: 1140px;
  }

  & .beathub-container {
    border: 1px solid;
    display: flex;
    padding: 1rem 3rem;
    min-width: 1140px;
  }

`

export default Wrapper