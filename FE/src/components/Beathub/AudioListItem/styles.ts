import styled from 'styled-components';

const Wrapper = styled.div`

  // border-top: 0.5px solid rgba(255, 255, 255, 0.2);
  background-color: rgba(33, 44, 79, 0.8);

  & .list-item {
    padding-right: 0;
    padding-left: 0;
  }
`;

export default Wrapper;
